<div id="user-bar-greeting">
  <% if @current_user %>
    Logged in as <%= @current_user.name %>. <%= link_to 'Log Out', logout_path %>
  <% else %>
    <%= link_to 'Log In', new_user_session_path %> or <%= link_to 'Register', new_user_registration_path 
    # :class => 'user-bar-action' %><%= ' via CalNet' unless session[:cas_user].present?
									%> to access our listings.
  <% end %>
</div>
